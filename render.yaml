services:
  - type: worker
    name: h1b-slot-watcher
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python monitor.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
